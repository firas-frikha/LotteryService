slick = {
  dbs = {
    query = {
      profile = "slick.jdbc.H2Profile$"
      db = {
        connectionPool = "slick.jdbc.hikaricp.HikariCPJdbcDataSource$"
        dataSourceClassName = "org.h2.jdbcx.JdbcDataSource"
        poolName = "query"
        queueSize = 500
        connectionTimeout = 15000
        validationTimeout = 3000
        idleTimeout = 60000
        maxLifetime = 600000
        leakDetectionThreshold = 0
        keepAliveConnection = on
        numThreads = 10
        maxConnections = 10
        minConnections = 2
        properties = {
          url = "jdbc:h2:mem:"${testDatabaseName}";MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS read_storage\\;SET SCHEMA read_storage;"
          user = "sa"
          password = "sa"
        }
      }
    }
  }
}